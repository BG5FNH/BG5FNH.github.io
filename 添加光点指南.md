# 添加光点指南

当前版本：v1.1.0

## 1. 名称约定

| 名称 | 指的是什么 |
|---|---|
| **导航点** | 首页主线上的大金色点，例如「个人介绍」「资料分享」 |
| **星图** | 点击导航点后进入的神经元树状图 |
| **光点** | 星图里的每一个金色小点 |

以后我们沟通时统一这样说：

```
首页 → 导航点 → 星图 → 光点 → 内容页面
```

---

## 2. 添加一个光点需要动哪些文件

### 最少需要 2 个地方

1. 新建光点页面：

```
WayPoints/对应导航点文件夹/新光点文件夹/index.html
```

2. 在导航点配置里注册：

```
points/waypoints.js
```

### 建议同步更新（版本规范）

- `pc/index.html`
- `mobile/index_m.html`
- `index.html`
- `README.md`
- `开发文档.md`
- `添加光点指南.md`

最后运行：

```
push.cmd
```

---

## 3. 第 1 步：写光点页面

### 普通文字页面

新建：

```
WayPoints/MyIntroduce/MyLightPoint/index.html
```

内容：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>我的光点 - BG5FNH</title>
  <script>window.BG5FNH_ROOT = "../../../";</script>
  <link rel="stylesheet" href="../../../assets/style.css" />
</head>
<body>
  <script>
    window.BG5FNH_PAGE = {
      title: "我的光点",
      subtitle: "个人介绍 · 我的光点",
      html: '<h2>我的光点</h2><p>这里写内容。</p>'
    };
  </script>
  <script src="../../../assets/page.js"></script>
</body>
</html>
```

注意 `BG5FNH_ROOT`：

- 页面深度 2：`../../`
- 页面深度 3：`../../../`
- 更深路径按层数增加 `../`

### 交互页面

如果需要按钮、滑动条、JS，参考：

```
WayPoints/MyIntroduce/ControlTest/index.html
```

交互页面需要自己写完整 HTML/CSS/JS，并保留：

```html
<button class="back-btn" id="backBtn">返回</button>
```

---

## 4. 第 2 步：在星图里注册光点

打开：

```
points/waypoints.js
```

找到对应导航点，例如「个人介绍」：

```js
{
  id: 'MyIntroduce',
  name: '个人介绍',
  points: [
    // 在这里添加
  ]
}
```

在 `points` 数组里添加：

```js
{
  path: 'MyIntroduce/MyLightPoint',
  name: '我的光点',
  pos: [1, 1],
  url: 'MyIntroduce/MyLightPoint/index.html'
}
```

### 字段解释

| 字段 | 作用 |
|---|---|
| `path` | 文件夹嵌套路径，决定这个光点连接在哪个父光点上 |
| `name` | 光点上显示的文字 |
| `pos` | 星图坐标 `[横, 纵]`，中心是 `[0, 0]` |
| `url` | 页面路径，相对于 `WayPoints/` 文件夹 |

### 坐标示例

| 位置 | `pos` |
|---|---|
| 中心 | `[0, 0]` |
| 右边一格 | `[1, 0]` |
| 左边一格 | `[-1, 0]` |
| 上边一格 | `[0, 1]` |
| 下边一格 | `[0, -1]` |
| 右上一格 | `[1, 1]` |

一格的大小由 `gridUnit` 控制，默认 `3.6`。

---

## 5. 添加嵌套光点

现在支持多级分叉。

示例：

```js
{ path: 'MyIntroduce/ParentLight', name: '父光点', pos: [0, 1], url: 'MyIntroduce/ParentLight/index.html' },
{ path: 'MyIntroduce/ParentLight/ChildLight', name: '子光点', pos: [1, 1], url: 'MyIntroduce/ParentLight/ChildLight/index.html' }
```

系统会根据 `path` 的文件夹嵌套自动连接：

```
MyIntroduce → ParentLight → ChildLight
```

---

## 6. 版本号更新

假设从 `v1.0.21` 升到 `v1.0.22`，需要同步修改：

`pc/index.html`：

```html
<div id="versionBadge">v1.0.22</div>
```

```html
<script src="js/three.min.js?v=1.0.22"></script>
<script src="../points/waypoints.js?v=1.0.22"></script>
<script src="js/main3d.js?v=1.0.22"></script>
```

`mobile/index_m.html`：

```html
<div id="versionBadge">v1.0.22</div>
```

```html
<script src="../pc/js/three.min.js?v=1.0.22"></script>
<script src="../points/waypoints.js?v=1.0.22"></script>
<script src="../pc/js/main3d.js?v=1.0.22"></script>
```

`index.html`：

```js
var target = (isMobile ? 'mobile/index_m.html?v=1.0.22' : 'pc/index.html?v=1.0.22');
```

`README.md`：

```markdown
**v1.0.22**
```

`开发文档.md` 和本指南里的版本号也一起改。

---

## 7. 推送

双击：

```
G:\DSharness工作区\BG5FNH网页代码\push.cmd
```

---

## 8. 添加光点检查清单

- [ ] `WayPoints/.../新光点/index.html` 已创建
- [ ] `points/waypoints.js` 已注册，id 不重复
- [ ] 逗号没有漏写
- [ ] `url` 路径写对
- [ ] 版本号已更新
- [ ] 运行了 `push.cmd`
- [ ] 手机和电脑都测试通过
