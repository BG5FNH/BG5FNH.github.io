# BG5FNH 个人网页 开发文档

当前版本：v1.1.1

本项目的 3D 主界面只是一个外壳，真正的文字页面全部放在 `WayPoints/` 文件夹里。
每个光点对应一个 `index.html`，点击后会被主界面加载到 iframe 中显示。

---

## 1. 整体目录结构

```
BG5FNH网页代码/
├─ index.html                 # 入口：判断设备，跳转 pc 或 mobile
├─ pc/
│  ├─ index.html              # 电脑端 3D 主界面
│  └─ js/
│     ├─ three.min.js         # 本地 three.js r128
│     ├─ waypoints.js         # ★ 光点、坐标、页面路径配置
│     └─ main3d.js            # 3D 场景、镜头、交互、字体缩放
├─ mobile/
│  ├─ index_m.html            # 手机端 3D 主界面（加载同一套 JS）
│  └─ ...
├─ WayPoints/                 # ★ 所有内容页面
│  ├─ MyIntroduce/
│  │  ├─ index.html
│  │  ├─ SoftwareLearning/index.html
│  │  ├─ ControlTest/index.html
│  │  └─ ...
│  ├─ ResourceShare/
│  ├─ TechArticles/
│  └─ MessageBoard/
├─ assets/
│  ├─ style.css               # 内容页通用黑金样式
│  └─ page.js                 # 内容页自动渲染 + 返回按钮逻辑
└─ README.md
```

---

## 2. 内容页 index.html 怎么写

### 方式 A：普通内容页（推荐，最简单）

适用于文字、标签、图片等普通内容。

文件位置示例：

```
WayPoints/MyIntroduce/index.html
```

内容：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>个人简介 - BG5FNH</title>
  <script>window.BG5FNH_ROOT = "../../";</script>
  <link rel="stylesheet" href="../../assets/style.css" />
</head>
<body>
  <script>
    window.BG5FNH_PAGE = {
      title: "个人简介",
      subtitle: "BG5FNH · 个人介绍",
      html: '<h2>肖和</h2><p>呼号：<strong>BG5FNH</strong></p><p>这里写介绍文字。</p><div class="tags"><span class="tag">标签1</span></div>'
    };
  </script>
  <script src="../../assets/page.js"></script>
</body>
</html>
```

说明：

- `BG5FNH_ROOT` 是回到项目根目录的相对路径。
  - 页面在 `WayPoints/某个文件夹/index.html`：写 `"../../"`
  - 页面在 `WayPoints/某个文件夹/子文件夹/index.html`：写 `"../../../"`
- `title`：页面大标题。
- `subtitle`：标题下方小字。
- `html`：卡片里显示的 HTML，用单引号包裹。
- `assets/page.js` 会自动生成：
  - 背景光点
  - 左上角返回按钮
  - 标题
  - 卡片

### 方式 B：独立交互页（适合按钮、滑动条、JS 控件）

参考：

```
WayPoints/MyIntroduce/ControlTest/index.html
```

这种页面自己写完整 HTML、CSS、JS。

必须保留：

```html
<button class="back-btn" id="backBtn">返回</button>
```

返回按钮逻辑：

```js
try {
  if (window.parent !== window) {
    window.parent.postMessage({ type: 'BG5FNH_CLOSE_OVERLAY' }, '*');
    return;
  }
} catch (e) {}
window.location.href = root + 'pc/index.html';
```

还要加嵌入检测，让页面背景透明：

```html
<script>
  (function () {
    try {
      if (window.parent !== window) {
        document.documentElement.classList.add('embedded');
        document.body.classList.add('embedded');
      }
    } catch (e) {
      document.documentElement.classList.add('embedded');
      document.body.classList.add('embedded');
    }
  })();
</script>
```

---

## 3. 怎么注册一个光点

所有光点都配置在：

```
points/waypoints.js
```

### 3.1 主线上的一级导航点

```js
{
  id: 'MyIntroduce',
  name: '个人介绍',
  x: -1.5,
  scale: 1.55,
  points: [ ... ]
}
```

字段：

| 字段 | 说明 |
|---|---|
| `id` | 唯一英文 id |
| `name` | 导航点显示文字 |
| `x` | 电脑端主线上 X 轴坐标 |
| `scale` | 导航点大小倍率 |
| `points` | 该导航点星图里的光点列表 |

### 3.2 星图光点

```js
{
  path: 'MyIntroduce/HardwareDevelopment',
  name: '硬件开发',
  pos: [1, 0],
  url: 'MyIntroduce/HardwareDevelopment/index.html'
}
```

字段：

| 字段 | 说明 |
|---|---|
| `path` | 文件夹嵌套路径，决定这个光点连接在哪个父光点上 |
| `name` | 光点显示文字 |
| `pos` | 星图坐标 `[横, 纵]`，中心是 `[0, 0]` |
| `url` | 页面路径，相对于 `WayPoints/` |

### 3.3 坐标规则

星图中心为 `[0, 0]`：

| 位置 | `pos` |
|---|---|
| 中心 | `[0, 0]` |
| 右边一格 | `[1, 0]` |
| 左边一格 | `[-1, 0]` |
| 上边一格 | `[0, 1]` |
| 下边一格 | `[0, -1]` |
| 右上一格 | `[1, 1]` |

父子关系由 `path` 自动判断：

```
MyIntroduce/SoftwareLearning
```

它的父光点是：

```
MyIntroduce
```

系统会自动画金色连线。

---

## 4. 常用可控制参数

### 4.1 版本号

修改这两个文件里的：

```html
<div id="versionBadge">v1.0.17</div>
```

- `pc/index.html`
- `mobile/index_m.html`

还要同步修改：

- `index.html` 里的跳转参数 `?v=...`
- `README.md` 里的版本号
- 两个主页面里的 JS 引用版本参数：
  - `pc/index.html`
  - `mobile/index_m.html`

### 4.2 手机端主视线

都在 `pc/js/main3d.js` 顶部附近：

```js
var MOBILE_TOP_Y = 5.0;      // 第一个光点的初始高度
var MOBILE_Y_STEP = 2.0;     // 手机端光点间距
var MOBILE_LINE_X = -2.0;    // 金色竖线在屏幕左/右位置
```

手机端金色线粗细：

搜索：

```js
var mobileThick = 0.08;
```

### 4.3 电脑端金色主线粗细

搜索：

```js
var mainLineThick =
```

电脑宽屏是 `0.22`，窄触屏是 `0.32`。

### 4.4 镜头距离 / 缩放

电脑端聚焦树状图镜头距离：

```js
var dist = 24;
```

手机端顶部视角高度：

```js
fy + 26
```

数值越大，树状图看起来越小，能看到更多光点。

### 4.5 动画速度

进入树状图：

```js
startAnimation(getFocusCamera(mainDef), mainDef, 2.6);
```

返回主视线：

```js
startAnimation(getOverviewCamera(), null, 2.2);
```

数字越大越慢。

### 4.6 文字大小

搜索 `worldFont`：

```js
var worldFont =
```

- 电脑端主界面文字倍率：`0.7 / 0.9`
- 树状图文字额外缩放：`0.8`
- 手机端文字额外缩放：`0.45`

### 4.7 背景模糊强度

在 `pc/index.html` 和 `mobile/index_m.html` 中搜索：

```css
.page-blur
```

`blur(4px)` 改成 `blur(2px)` 更轻，改成 `blur(6px)` 更重。

---

## 5. 修改后如何发布

双击运行：

```
G:\DSharness工作区\BG5FNH网页代码\push.cmd
```

它会自动 add、commit、push。

---

## 6. 常见问题

### 页面打开后看不到背景神经元

独立页面没有加载 `assets/page.js`，需要手动添加 `embedded` 类检测脚本，见第 2 节方式 B。

### 改了代码但线上没变化

1. 运行 `push.cmd`。
2. 确认版本号已经更新。
3. 访问时带 `?v=版本号` 强制刷新。
4. 手机清除缓存或使用无痕模式。

### 页面无法打开 / 黑屏

先检查：

- 是否出现 Git 冲突标记：
  ```
  <<<<<<<
  =======
  >>>>>>>
  ```
- `waypoints.js` 每个子节点之间是否有逗号。
- 浏览器按 F12 看 Console 红色报错。

---

## 7. 新增页面快速步骤

1. 在 `WayPoints/` 下新建文件夹。
2. 放入 `index.html`，按第 2 节方式 A 或 B 编写。
3. 在 `points/waypoints.js` 里注册光点。
4. 更新版本号。
5. 运行 `push.cmd`。
